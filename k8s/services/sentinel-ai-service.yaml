apiVersion: v1
kind: Service
metadata:
  name: sentinel-ai-service
  namespace: sentinelops
spec:
  selector:
    app: sentinel-ai
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP